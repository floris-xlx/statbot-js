# Get top message channels | Statbot

# Get top message channels

GET 

## https://api.statbot.net/v1/guilds/:guild\_id/messages/tops/channels

Get top message channels for a guild.

## Request[​](#request "Direct link to Request")

### Path Parameters

**guild\_id** snowflakerequired

### Query Parameters

**start** unix-timestamp

**Possible values:** `>= 0`

Minimum timestamp in milliseconds since epoch.

**timezone\_offset** number

**Possible values:** `>= -12` and `<= 14`

Timezone offset in hours. For example, `-3` for UTC-3.

**interval** enum

**Possible values:** \[`hour`, `day`, `week`, `month`\]

Time interval for the query. Defaults to `day`.

**end** unix-timestamp

**Possible values:** `>= 0`

Maximum timestamp in milliseconds since epoch. If not provided, the current time is used.

**bot** boolean

If `true`, the query will only include bot users. If `false`, it will only include non-bot users. If not provided, both bots and non-bots will be included.

**whitelist\_members\[\]** string\[\]

**Possible values:** Value must match regular expression `^[0-9]+$`

List of member Discord IDs to whitelist. If provided, only these members will be included in the results. Cannot be used with `blacklist_members`.

**blacklist\_members\[\]** string\[\]

**Possible values:** Value must match regular expression `^[0-9]+$`

List of member Discord IDs to blacklist. If provided, these members will be excluded from the results. Cannot be used with `whitelist_members`.

**whitelist\_roles\[\]** string\[\]

**Possible values:** Value must match regular expression `^[0-9]+$`

List of role Discord IDs to whitelist. If provided, members with at least one of these roles will be included in the results. This uses members' current roles. Members without roles will not be included.

**blacklist\_roles\[\]** string\[\]

**Possible values:** Value must match regular expression `^[0-9]+$`

List of role Discord IDs to blacklist. If provided, members with any of these roles will be excluded from the results. This uses members' current roles. All members without roles will be included.

**whitelist\_channels\[\]** string\[\]

**Possible values:** Value must match regular expression `^[0-9]+$`

List of channel Discord IDs to whitelist. If provided, only these channels will be included in the results. Cannot be used with `blacklist_channels`.

**blacklist\_channels\[\]** string\[\]

**Possible values:** Value must match regular expression `^[0-9]+$`

List of channel Discord IDs to blacklist. If provided, these channels will be excluded from the results. Cannot be used with `whitelist_channels`.

**whitelist\_voice\_channels\[\]** string\[\]

**Possible values:** Value must match regular expression `^[0-9]+$`

A separate list of voice channel Discord IDs to whitelist. If provided, voice stats will be filtered by these channels instead of the other channel filters. Cannot be used with `blacklist_voice_channels`.

**blacklist\_voice\_channels\[\]** string\[\]

**Possible values:** Value must match regular expression `^[0-9]+$`

A separate list of voice channel Discord IDs to blacklist. If provided, voice stats will be filtered by these channels instead of the other channel filters. Cannot be used with `whitelist_voice_channels`.

**limit** integer

**Possible values:** `>= 1`

Maximum number of results to return. Cannot be used with `page_size`, `page`, or `select`.

**order** enum

**Possible values:** \[`asc`, `desc`\]

Order of the results. Can be either `asc` for ascending or `desc` for descending. Sorts by `timestamp` for series data, and by `rank` for top data.

**page\_size** integer

**Possible values:** `>= 1`

Number of results per page. Requires `page` to be set. Cannot be used with `limit` or `select`.

**page** integer

**Possible values:** `>= 1`

Page number for pagination. Requires `page_size` to be set. Cannot be used with `limit` or `select`.

**select\[\]** string\[\]

**Possible values:** Value must match regular expression `^[0-9]+$`

Discord IDs to select in the top query. If provided, only these IDs will be included in the results. Results are not guaranteed to contain all specified IDs. Cannot be used with `limit`, `page_size`, or `page`.

**full** boolean

If `true`, the full optional fields will be included in the response. Otherwise, only the required fields will be included.

## Responses[​](#responses "Direct link to Responses")

-   200
-   401

A list of top message channels for a guild. Returns a list of `PartialTopChannel` by default. Set `full=true` to return `TopChannel`.

**Response Headers**

**x-ratelimit-limit**

Maximum number of requests that can be made in the current time window.

**x-ratelimit-remaining**

Number of requests remaining in the current time window.

**x-ratelimit-reset**

How many seconds until the rate limit resets.

**retry-after**

If max requests are reached, the time in seconds to wait before making a new request.

-   application/json

-   Schema
-   Example (auto)
-   TopPartialChannelList
-   TopChannelList

**Schema**

oneOf

-   PartialTopChannelList
-   TopChannelList

-   Array \[
    

**rank**integerrequired

Rank in data set.

**count**integerrequired

Number of messages.

**memberCount**integerrequired

Number of members that participated in the channel.

-   \]
    

-   Array \[
    
anyOf

-   TopChannel
-   TopAggregateChannel

**rank**integerrequired

Rank in data set.

**count**integerrequired

Number of messages.

**memberCount**integerrequired

Number of members that participated in the channel.

**id**string<snowflake>required

Discord ID of the channel.

**name**stringrequired

Name of the channel.

**Example:** `general`

**type**integer<enum>required

Type of channel. [Discord Channel type](https://discord.com/developers/docs/resources/channel#channel-object-channel-types), or `999` for Statbot Aggregate Channels.

**parentId**string<snowflake>nullablerequired

Discord ID of the parent channel, if any.

**isActive**integer<enum>required

If the channel still exists in Discord.

ENUM VALUE

DESCRIPTION

0

Channel has been deleted.

1

Channel is active.

**rank**integerrequired

Rank in data set.

**count**integerrequired

Number of messages.

**memberCount**integerrequired

Number of members that participated in the channel.

**id**string<snowflake>required

Discord ID of the channel.

**name**stringrequired

Name of the channel.

**Example:** `general`

**type**integer<enum>required

Type of channel. [Discord Channel type](https://discord.com/developers/docs/resources/channel#channel-object-channel-types), or `999` for Statbot Aggregate Channels.

**parentId**string<snowflake>nullablerequired

Discord ID of the parent channel, if any.

**aggregate**booleanrequired

Whether the channel is an aggregate channel.

**autoMergeEnabled**booleanrequired

Whether auto-merge is enabled for this Aggregate Channel.

**autoMergeChannelIds**string<snowflake>\[\]required

List of channel IDs that data is auto-merged from.

**autoMergeTypes**string<enum>\[\]required

List of data types to auto-merge.

**Possible values:** \[`text`, `voice`\]

**Example:** `["text","voice"]`

**createdUnixTimestamp**integer<unix-timestamp>required

Unix timestamp in ms when the Aggregate Channel was created.

-   \]
    

```
[  {    "rank": 1,    "count": 100,    "memberCount": 10  }]
```

Partial top channels list

```
[  {    "rank": 1,    "count": 100,    "memberCount": 10  }]
```

Top channels list

```
[  {    "rank": 1,    "count": 100,    "memberCount": 10,    "id": "123456789012345678",    "name": "general",    "type": 0,    "parentId": "123456789012345678",    "isActive": 1  },  {    "rank": 1,    "count": 100,    "memberCount": 10,    "id": "123456789012345678",    "name": "general",    "type": 0,    "parentId": "123456789012345678",    "aggregate": true,    "autoMergeEnabled": true,    "autoMergeChannelIds": [      "123456789012345678",      "987654321098765432"    ],    "autoMergeTypes": [      "text",      "voice"    ],    "createdUnixTimestamp": 1633072800000  }]
```

Unauthorized

-   application/json

-   Schema
-   Example (auto)

**Schema**

**message**stringrequired

```
{  "message": "string"}
```

#### Authorization: http

**name:** [BearerAuth](/docs/api/statbot-api#authentication)**type:** http**scheme:** bearer

-   csharp
-   curl
-   dart
-   go
-   http
-   java
-   javascript
-   kotlin
-   c
-   nodejs
-   objective-c
-   ocaml
-   php
-   powershell
-   python
-   r
-   ruby
-   rust
-   shell
-   swift

-   HTTPIE
-   WGET

```
http --follow --timeout 3600 GET 'https://api.statbot.net/v1/guilds/:guild_id/messages/tops/channels' \ Accept:'application/json' \ Authorization:'Bearer <token>'
```

Request Collapse all

Base URL

Edit

https://api.statbot.net

Auth

Bearer Token

Parameters

guild\_id — pathrequired

Show optional parameters

start — query

timezone\_offset — query

interval — query

\---hourdayweekmonth

end — query

bot — query

\---truefalse

whitelist\_members\[\] — query

Add item

blacklist\_members\[\] — query

Add item

whitelist\_roles\[\] — query

Add item

blacklist\_roles\[\] — query

Add item

whitelist\_channels\[\] — query

Add item

blacklist\_channels\[\] — query

Add item

whitelist\_voice\_channels\[\] — query

Add item

blacklist\_voice\_channels\[\] — query

Add item

limit — query

order — query

\---ascdesc

page\_size — query

page — query

select\[\] — query

Add item

full — query

\---truefalse

Send API Request

ResponseClear

Click the `Send API Request` button above and see the response here!